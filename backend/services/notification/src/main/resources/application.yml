server:
  port: ${SERVER_PORT:8083}

spring:
  application:
    name: smartdoc-notification
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

logging:
  level:
    root: INFO
    com.smartdoc.notification: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info

apiVersion: v1
kind: Service
metadata:
  name: smartdoc-analysis
  namespace: smartdoc
spec:
  selector:
    app: smartdoc-analysis
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  type: ClusterIP

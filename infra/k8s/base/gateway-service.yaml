apiVersion: v1
kind: Service
metadata:
  name: smartdoc-gateway
  namespace: smartdoc
spec:
  selector:
    app: smartdoc-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: smartdoc-notification
  namespace: smartdoc
spec:
  selector:
    app: smartdoc-notification
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  type: ClusterIP

apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudwatch-agent
  namespace: amazon-cloudwatch
  annotations:
    # EKS IRSA (recommended): replace with IAM role ARN that allows CloudWatch writes.
    eks.amazonaws.com/role-arn: REPLACE_ME_IRSA_ROLE_ARN
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: fluent-bit
  namespace: amazon-cloudwatch
  annotations:
    # EKS IRSA (recommended): replace with IAM role ARN that allows CloudWatch Logs writes.
    eks.amazonaws.com/role-arn: REPLACE_ME_IRSA_ROLE_ARN

